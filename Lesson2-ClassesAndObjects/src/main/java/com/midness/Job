У нас есть класс PersonModel, он хранит пароль, имя, возраст пользователя.

В классе PersonModel есть метод getUserInfo, которая выдаёт строку с информацией о пароле, имени и возрасте
(аля, Пользователь Доня возрастом 19 лет, пароль - 12345)

Также в PersonModel должен быть метод который создаёт новый объект PersonModel, и вставляет в него данные из старого объекта.
То есть создаёт и возвращает PersonModel, с теми же данными (пароль, имя, возраст).

"public PersonModel copyPerson()"

new PersonModel();

Нужно чтобы создалось 3 объекта класса PersonModel, с любыми значениями пароля имени и возраста.

1. PersonModel Хранит - Пароль,имя,возраст.
2. В классе есть метод getUserInfo, которая выдаёт строку с информацией о пароле, имени и возрасте
3. Класс PersonModel должен создавать новый такой-же объект и вставляет в него данные из старого объекта.